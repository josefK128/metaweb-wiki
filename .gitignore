# Include your project-specific ignores in this file
# Read about how to use .gitignore: 
# https://help.github.com/articles/ignoring-files
# in the discussion below root is the clone root, i.e
# the directory which contains .gitignore (this file)


# NOTE: ./node_modules can be built from package.json
# they are dev-dependencies only
# build node_modules locally from package.json by:
# root> npm install --savedev
./node_modules/*

# NOTE: ./app/libs may be re-built from bower.json
# they are app-dependencies
# app/libs is re-built from bower.json by: 
# root> bower install --S

# NOTE: ./app/build can be re-built by:
# root> gulp build
# root> gulp build-min

# NOTE: ./docs can be re-built in by
# root> gulp docco

# NOTE: ./history can be incrementally populated by
# root> gulp save-versions

# text-notes in ./app
./app/*.txt

# vim swp-files
*.swp


# the following are all not expected to appear but included for completeness
# Compiled source 
*.com
*.class
*.dll
*.exe
*.o
*.so
 
# Packages 
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
